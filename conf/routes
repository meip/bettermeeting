# Routes
# This file defines all application routes (Higher priority routes first)
#
# Note: @controllers syntax signifies the actual controller instance, retrieved via GlobalSettings.getController().
# ~~~~

# Single endpoint for serving AngularJS
GET     /                               @controllers.Application.index
GET     /api/randomUUID                 @controllers.Application.randomUUID

# User management API
GET     /api/users                      @controllers.Users.findUsers
POST    /api/user                       @controllers.Users.createUser

# Meeting management API
GET         /api/meetings               @controllers.Meetings.list
POST        /api/meetings               @controllers.Meetings.create
DELETE      /api/meetings/:id           @controllers.Meetings.delete(id)
GET         /api/meetings/:id           @controllers.Meetings.get(id)
PUT         /api/meetings/:id           @controllers.Meetings.update(id)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

